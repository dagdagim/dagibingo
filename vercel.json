{
  "framework": "vite",
  "buildCommand": "npm run build --workspace=@bingo/client",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
